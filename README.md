# Portrait Prompt Browser

A static browser for portrait prompts with filtering, pagination, random prompt mode, and one-click copy actions.

## Local Preview

Open `prompt-browser/index.html` directly in a browser.

## Deployment

GitHub Pages is deployed from the `prompt-browser` directory through the workflow in `.github/workflows/deploy-pages.yml`.
